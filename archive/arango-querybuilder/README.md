# ARCHIVE — not part of the OpenPeeps runtime

Historical ArangoDB query builder used by cutover / data-migration history.
Do not import this from runtime packages (`@openpeepshq/core`, server, worker).

See `archive/README.md` and `@openpeepshq/arango-migrate`.
