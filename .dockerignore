.media
.home
.pnpm-store

node_modules

# Build outputs — the in-container build (pnpm -r build) produces these fresh.
# Excluding them avoids leaking stale local artifacts into the image and keeps
# the build deterministic.
**/dist
**/build
**/*.tsbuildinfo

# Test reports / coverage
**/coverage
**/test-results
**/playwright-report

# Editor / OS noise
.vscode
.idea
.DS_Store
